#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main() {
  int arr[] = {6, 5, 4, 3, 2, 1};
  int n = sizeof(arr) / sizeof(arr[0]);
  for (int i = 0; i < n - 1; i++) {
    cout << arr[i] << endl;
  }
  cout << "Hi Aakash lets start with neovim" << endl;
  return 0;
}
